export interface REQUEST_DATA {
  error: any;
  isLoading: boolean;
}

export interface RESULTS_DATA {
  nResults: number;
  data: any;
}

export interface FAVORITE_CHARACTER_DATA {
  id: number;
  name: string;
  img: string;
}
